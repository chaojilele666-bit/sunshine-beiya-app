# 当前项目状态

## 分支

- 当前分支：`V3`
- 不修改 `main`。
- 不修改 `V2`。
- 不恢复现有 stash，除非用户明确要求。

## 当前工作区

当前工作区存在 V3/V3.1 相关未提交修改，覆盖后台、前端、数据库迁移和文档。

已知 stash：

- `stash@{0}: On V2: wip: home changes and backend audit`

## 已实现

- PostgreSQL 后台资源：账号、阿姨、客户需求、预约、接单申请、订单、派单、门店、服务中心和首页轮播。
- 后台真实登录、session token、后端权限判断。
- 后台对外身份名称：运营端、管理端。
- 后台 hash 路由模块页。
- 管理端经营看板和操作日志。
- `/api/miniprogram` 小程序数据接口。
- 门店列表、门店详情、电话、导航、门店图片和推荐阿姨。
- 本地 HTTP 图片转换为微信本地文件显示。
- 客户端首页公司展示轮播。
- 客户端首页服务中心，来自 `serviceModules`。
- V3.1 客户需求提交、推荐阿姨、确认/拒绝匹配。

## 已自动检查

- 部分 JS 文件使用 Docker Node 进行了 `node --check`。
- 部分 JSON 文件做了解析检查。
- 部分 WXML 做了标签闭合检查。
- 执行过 `git diff --check`。
- 本地 PostgreSQL V006 已应用并验证过 `/api/miniprogram` 返回 `serviceModules`。

## 待人工验证

- 微信开发者工具中客户端首页服务中心视觉效果。
- 后台服务中心上传 `iconImage` 后小程序同步显示。
- V3.1 需求详情、推荐列表、确认/拒绝流程。
- 后台 hash 路由每个模块页面是否互斥显示。

## 风险

- 当前 V3.1 需求访问依赖本地保存的 accessToken，正式微信登录未接入前无法跨设备恢复。
- 本地图片 URL 方案只适合开发者工具，正式上线必须使用 HTTPS 文件服务或云存储。
- V001 标准化表不是当前主业务表，不要和 V2/V3 资源表混用。
